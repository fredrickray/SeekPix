"""OCR placeholder — Phase 2 / later."""

from __future__ import annotations

from pathlib import Path


def extract_text(path: str | Path) -> str:
    """Reserved for EasyOCR in a later phase."""
    raise NotImplementedError("OCR is not enabled in this build phase")
